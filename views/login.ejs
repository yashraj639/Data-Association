<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Data Association</title>
  </head>
  <body>
    <div
      class="main w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center"
    >
      <form
        class="flex flex-col items-center justify-center gap-5"
        action="/login"
        method="post"
      >
        <h1 class="text-2xl">Hey, welcome back 👋</h1>

        <input
          class="px-3 py-2 bg-transparent outline-none rounded-md border-2 border-zinc-700"
          type="email"
          placeholder="email"
          name="email"
        />
        <input
          class="px-3 py-2 bg-transparent outline-none rounded-md border-2 border-zinc-700"
          type="password"
          placeholder="password"
          name="password"
        />
        <input
          class="px-3 py-2 bg-transparent outline-none rounded-md border-2 border-zinc-700 hover:bg-white hover:text-black transition-all duration-300 ease-in-out"
          type="submit"
          value="Sign In"
        />
      </form>
    </div>
  </body>
</html>
